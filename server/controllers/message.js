const mailer = require('../middleware/mailer');

const SendEmail = async(req,res,next) => {
    try {
        const {
            object, text
        }=req.body;
        const admin = req.user;
        
    } catch (error) {
        
    }
}